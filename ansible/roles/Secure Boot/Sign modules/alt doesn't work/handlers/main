---

- name: Reboot and enroll MOK Key
  become: true
  ansible.builtin.reboot:
    reboot_timeout: 3600

- name: Sign modules after reboot
  become: true
  ansible.builtin.command: /root/module-signing/sign-vbox-modules
